import { describe, it } from "vitest";

describe("debug", () => {
  it.todo("sends a preview to the server");
  it.todo("sets the current date time");
  it.todo("sets the alias");
  it.todo("sets the html");
  it.todo("sets the current context");
});
